---
interface Props {
  stacked?: boolean;
}
const { stacked = false } = Astro.props;
const messengers = [
  {
    name: "Telegram",
    href: "https://telegram.me/lenremont_bot",
    qr: "/assets/social/lr-qr-tg.webp",
  },
  {
    name: "MAX",
    href: "https://max.ru/id471902475874_3_bot",
    qr: "/assets/social/lr-qr-max.webp",
  },
  {
    name: "VK",
    href: "https://vk.ru/lenremont",
    qr: "/assets/social/lr-qr-vk.webp",
  },
];
---

<div class:list={["messenger-cards", { "messenger-cards--stacked": stacked }]}>
  {
    messengers.map(({ name, href, qr }) => (
      <a
        class:list={["hero-messenger", `hero-messenger--${name.toLowerCase()}`]}
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        aria-label={`Открыть ${name}`}
      >
        <strong>{name}</strong>
        <span class="hero-messenger__action">
          Отправить фото <span aria-hidden="true">↗</span>
        </span>
        <img
          src={qr}
          alt={`QR-код ${name} Ленремонт`}
          width="140"
          height="140"
          loading="lazy"
        />
      </a>
    ))
  }
</div>

<style>
  .messenger-cards {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 20px;
  }
  .hero-messenger {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 140px;
    grid-template-rows: 1fr 1fr;
    gap: 12px 16px;
    min-width: 0;
    padding: 20px;
    border: 1px solid #ffffff40;
    border-radius: 12px;
    color: white;
    background: linear-gradient(#ffffff0d, #ffffff0d), var(--night);
    transition:
      filter 0.2s,
      border-color 0.2s;
  }
  .hero-messenger--telegram {
    --messenger-background: linear-gradient(105deg, #51beec, #38a4d5);
  }
  .hero-messenger--vk {
    --messenger-background: linear-gradient(105deg, #3196fa, #0069df);
  }
  .hero-messenger--max {
    --messenger-background: linear-gradient(105deg, #b650ff, #9029e8);
  }
  .hero-messenger:hover {
    filter: brightness(1.08);
    border-color: #ffffff80;
  }
  .hero-messenger strong {
    grid-column: 1;
    grid-row: 1;
    align-self: center;
    font-size: 24px;
    line-height: 1.2;
  }
  .hero-messenger__action {
    grid-column: 1;
    grid-row: 2;
    align-self: end;
    color: white;
    font-size: 15px;
    font-weight: 650;
  }
  .hero-messenger img {
    grid-column: 2 / 3;
    grid-row: 1 / 3;
    width: 140px;
    height: 140px;
    padding: 6px;
    border-radius: 8px;
    background: white;
    object-fit: contain;
  }
  .messenger-cards--stacked {
    grid-template-columns: 1fr;
    gap: 12px;
  }
  @media (max-width: 1050px) {
    .messenger-cards {
      grid-template-columns: 1fr;
      gap: 12px;
    }
    .hero-messenger {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      min-height: 56px;
      padding: 15px 20px;
      background: var(--messenger-background);
    }
    .hero-messenger strong {
      font-size: 19px;
    }
    .hero-messenger img,
    .hero-messenger__action {
      display: none;
    }
    .hero-messenger::after {
      content: "↗";
      font-size: 22px;
      line-height: 1;
    }
  }
</style>
