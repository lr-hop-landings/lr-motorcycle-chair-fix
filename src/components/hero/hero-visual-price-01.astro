---
interface Pill {
  text: string;
}

interface Props {
  headline: string;
  accent?: string;
  subline: string;
  cta: string;
  ctaHref?: string;
  priceBadge?: string;
  pills?: Pill[];
  imageSrc?: string;
  imageAlt?: string;
  backgroundImageSrc?: string;
}

const {
  headline,
  accent,
  subline,
  cta,
  ctaHref = "#contacts",
  priceBadge,
  pills = [],
  imageSrc,
  imageAlt = "",
  backgroundImageSrc,
} = Astro.props;
---

<section class="hero-visual" style={backgroundImageSrc ? `--hero-bg-image: url("${backgroundImageSrc}")` : undefined}>
  <div class="hero-visual__inner">
    <div class="hero-visual__content">
      {priceBadge && <a class="hero-visual__badge" href={ctaHref}>{priceBadge}</a>}
      <h1 class="hero-visual__headline">
        {headline}
        {accent && <span>{accent}</span>}
      </h1>
      <p class="hero-visual__subline">{subline}</p>
      <div class="hero-visual__actions">
        <a class="hero-visual__button" href={ctaHref}>{cta}</a>
      </div>
      {pills.length > 0 && (
        <ul class="hero-visual__pills" aria-label="Key benefits">
          {pills.map((pill) => <li>{pill.text}</li>)}
        </ul>
      )}
    </div>
    {imageSrc ? (
      <a class="hero-visual__media glightbox" href={imageSrc} data-gallery="hero-visual" data-title={headline} aria-label={imageAlt || "Открыть изображение первого экрана"}>
        <img src={imageSrc} alt={imageAlt} loading="eager" />
      </a>
    ) : (
      <div class="hero-visual__media" aria-label="Плейсхолдер изображения">
        <div class="hero-visual__image-placeholder"></div>
      </div>
    )}
  </div>
</section>

<style>
  .hero-visual {
    position: relative;
    overflow: hidden;
    padding: var(--spacing-section, 96px) var(--space-4, 16px);
    color: var(--color-text, #000000);
    background: var(--color-background, #ffffff);
    background-size: cover;
    background-position: center;
  }

  .hero-visual__inner {
    width: min(100%, var(--container-width, 1180px));
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(320px, 1fr);
    gap: var(--space-16, 64px);
    align-items: center;
  }

  .hero-visual__content {
    position: relative;
    z-index: 1;
  }

  .hero-visual__badge {
    display: inline-flex;
    width: fit-content;
    margin-bottom: var(--space-6, 24px);
    padding: 10px 18px;
    border-radius: var(--radius-full, 999px);
    background: var(--color-primary, #000000);
    color: var(--color-white, #ffffff);
    font-family: var(--font-body, sans-serif);
    font-size: var(--text-sm, 14px);
    font-weight: var(--font-semibold, 600);
    text-decoration: none;
  }

  .hero-visual__headline {
    margin: 0;
    max-width: 780px;
    color: var(--color-text, #000000);
    font-family: var(--font-heading, sans-serif);
    font-size: clamp(36px, 6vw, 68px);
    line-height: var(--leading-tight, 1.08);
    font-weight: var(--font-bold, 700);
    letter-spacing: 0;
  }

  .hero-visual__headline span {
    display: block;
    color: var(--color-primary, #000000);
  }

  .hero-visual__subline {
    max-width: 620px;
    margin: var(--space-6, 24px) 0 0;
    color: var(--color-text-muted, #000000);
    font-family: var(--font-body, sans-serif);
    font-size: var(--text-lg, 18px);
    line-height: var(--leading-relaxed, 1.65);
  }

  .hero-visual__actions {
    margin-top: var(--space-8, 32px);
  }

  .hero-visual__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 54px;
    padding: 0 28px;
    border-radius: var(--radius-button, 14px);
    background: var(--color-accent, #000000);
    color: var(--color-white, #ffffff);
    font-family: var(--font-body, sans-serif);
    font-size: var(--text-base, 16px);
    font-weight: var(--font-semibold, 600);
    text-decoration: none;
    box-shadow: var(--shadow-hard, 6px 6px 0 #000000);
  }

  .hero-visual__pills {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-3, 12px);
    max-width: 560px;
    margin: var(--space-10, 40px) 0 0;
    padding: 0;
    list-style: none;
  }

  .hero-visual__pills li {
    display: flex;
    align-items: center;
    gap: var(--space-2, 8px);
    min-height: 46px;
    padding: 12px 16px;
    border-radius: var(--radius-full, 999px);
    background: var(--color-background, #ffffff);
    color: var(--color-text, #000000);
    font-family: var(--font-body, sans-serif);
    font-size: var(--text-sm, 14px);
    box-shadow: var(--shadow-hard, 6px 6px 0 #000000);
  }

  .hero-visual__pills li::before {
    content: "";
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--color-primary, #000000);
    flex: 0 0 auto;
  }

  .hero-visual__media {
    color: inherit;
    text-decoration: none;
  }

  .hero-visual__media img {
    width: 100%;
    height: auto;
    display: block;
    object-fit: contain;
  }

  .hero-visual__image-placeholder {
    position: relative;
    min-height: 360px;
    border: 2px solid #000000;
    background: #e5e5e5;
  }

  .hero-visual__image-placeholder::before {
    position: absolute;
    inset: 28%;
    border: 2px solid #000000;
    content: "";
  }

  .hero-visual__image-placeholder::after {
    position: absolute;
    top: 34%;
    left: 34%;
    width: 38px;
    height: 38px;
    border: 2px solid #000000;
    border-radius: 50%;
    background: #ffffff;
    content: "";
  }

  @media (max-width: 820px) {
    .hero-visual {
      padding-top: var(--space-16, 64px);
      padding-bottom: var(--space-16, 64px);
    }

    .hero-visual__inner {
      grid-template-columns: 1fr;
      gap: var(--space-8, 32px);
    }

    .hero-visual__media {
      order: -1;
    }

    .hero-visual__pills {
      grid-template-columns: 1fr;
    }

    .hero-visual__button {
      width: 100%;
    }
  }
</style>
