<div class="types">
  <div class="wrap">
    <span class="eyebrow">Для вашей техники</span>
    <span>Мотоциклы</span>
    <span>Скутеры</span>
    <span>Квадроциклы</span>
    <span>Багги</span>
  </div>
</div>

<style>
  .types {
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: 23px 0;
  }
  .types .wrap {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 24px;
  }
  .types span {
    font-size: 18px;
    font-weight: 700;
  }
  .types .eyebrow {
    font-size: 12px;
    color: var(--muted);
  }
  @media (max-width: 760px) {
    .types { padding: 20px 0; }
    .types .wrap { gap: 12px 22px; }
    .types .eyebrow { flex-basis: 100%; font-size: 11px; }
    .types span { font-size: 16px; }
  }
</style>
