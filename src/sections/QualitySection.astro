<section class="quality section" aria-labelledby="quality-title">
  <div class="container quality__grid">
    <div>
      <p class="eyebrow">КОНТРОЛЬ СОГЛАСОВАННОГО РЕЗУЛЬТАТА</p>
      <h2 id="quality-title">Проверяем посадку чехла, швы и согласованный дизайн</h2>
    </div>
    <ul class="check-list">
      <li>Состав работ зафиксирован</li>
      <li>Материал и дизайн согласованы</li>
      <li>Заказ проверен перед выдачей</li>
      <li>
        Гарантия 1 год заявлена на официальной странице; условия уточняются в
        документах к заказу
      </li>
    </ul>
  </div>
</section>
<style>
  .quality {
    padding-block: clamp(68px, 9vw, 126px);
    background: #e8e5df;
  }
  .quality__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(40px, 9vw, 155px);
    align-items: start;
  }
  .quality h2 {
    margin-bottom: 0;
  }
  .check-list {
    display: grid;
    gap: 13px;
    margin: 16px 0 0;
    padding: 16px 0 0;
    border-top: 1px solid var(--line);
    list-style: none;
  }
  .check-list li {
    position: relative;
    padding-left: 27px;
  }
  .check-list li::before {
    position: absolute;
    top: 0.52em;
    left: 0;
    width: 13px;
    height: 2px;
    background: var(--orange);
    content: "";
  }
  @media (max-width: 900px) {
    .quality__grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
  }
</style>
