# URL в формате peretyazhka-sidenij-mototransporta

18 сентября 2026, локальный testing-gutenberg, плагин 0.5.1. Production не изменялся.

Изменены только пять страниц: ID 11, 19, 20, 21, 22. Новый общий префикс — `/peretyazhka-sidenij-mototransporta/`; дочерние страницы используют суффиксы `-motocikly`, `-skutery`, `-kvadrocikly`, `-baggi` перед завершающим `/`.

Перед изменением сохранён WXR `before-mototransporta-urls-2026-09-18.xml`. Предварительный запуск `ops/prepare-pages.php dry-run --user=admin` подтвердил отсутствие конфликтов и правильные ID. Затем выполнен `apply` с явным `--path` локального WordPress. Оба запуска завершились с кодом 0.

Обновлены ссылки в существующем содержимом Gutenberg, пути квиза и служебных форм, каталог и пять JSON-манифестов. Повторный импорт содержимого не выполнялся. Сравнение с резервной копией подтвердило побайтовое совпадение `post_content`, за исключением запланированных подстановок URL, для всех пяти страниц.

Проверки завершились успешно, код 0:

- `ops/verify-pages.php`: пять HTTP 200, правильные canonical, все 20 взаимных ссылок, якоря, пути форм и квиза; старых взаимных ссылок не осталось.
- Шесть старых адресов (два у главной и по одному у страниц техники) отвечают 301 сразу на соответствующий конечный URL, без цепочки.
- На главной осталось 468 блоков, на страницах техники по 418; на каждой по 13 секций.
- `tests/converter.test.cjs`, `tests/validate-manifest.mjs`, `tests/wp-smoke.php`.
- Исходники плагина и установленная копия совпадают по хешам.

Собран новый `lenremont-page-importer-0.5.1.zip`; предыдущие архивы сохранены. Видимая вёрстка и логика квиза в этом изменении не затрагивались. Несвязанное предупреждение Local о `php_imagick.dll` оставлено без изменений.
